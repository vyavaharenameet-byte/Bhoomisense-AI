# BhoomiSense frontend — environment variables (copy to .env)
#
# In DEVELOPMENT leave this blank: vite.config.ts proxies /api -> localhost:8000.
# In PRODUCTION set it to your deployed backend URL (Render/Railway).
VITE_API_URL=
# e.g. VITE_API_URL=https://bhoomisense-api.onrender.com
